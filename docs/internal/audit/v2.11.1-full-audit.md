# V2.11.1 全量审查文档

> 累计回看审查：V2.9 → V2.9.1 → V2.10 → V2.11 → V2.11.1
> 目标：找出所有 bug 和问题，确保后续版本可持续开发

---

## 审查范围

### A. 组合引擎 (ez/portfolio/engine.py)
- [x] A1. 会计不变量：每日 cash + Σ(shares×price) == equity（当前 assert 同义反复，是否有实际验证？）
- [x] A2. 卖先买后：sell 阶段是否完整结束后才开始 buy？中间状态是否正确？
- [x] A3. 离散股数：weight→amount→shares 整手取整→余额回现金，是否有精度丢失？
- [x] A4. T+1：sold_today 集合是否每日清空？跨日是否泄漏？
- [x] A5. 方向性滑点：buy 推高 / sell 推低，slippage_rate=0 时是否完全无影响？
- [x] A6. 涨跌停检查：用 raw close（非 adj），tolerance 1e-6，是否覆盖科创板 20%？
- [x] A7. NaN 柱防护：has_bar_today 要求 raw_close 有效，如果 open/high/low 有效但 close 为 NaN？
- [x] A8. 基准曲线：NaN per-index fallback 是否正确？基准为空字符串时是否全零？
- [x] A9. 首日 prev_raw_close 初始化：是否从 pre-index 数据正确取值？
- [x] A10. pre-index sort guard：未排序数据是否被检测并报错？
- [x] A11. 引擎确定性：相同输入两次运行结果是否完全一致？（浮点精度影响？）
- [x] A12. 空 universe：所有股票都被跳过时，equity_curve 是否为 [initial_cash, ...]？

### B. 截面因子 (ez/portfolio/cross_factor.py + ez/factor/builtin/fundamental.py)
- [x] B1. compute_raw() 向后兼容：用户只实现 compute() 的旧因子，compute_raw() 是否正确 fallback？
- [x] B2. compute_raw() 递归：新因子 compute() 调 compute_raw()，compute_raw() 默认调 compute()——是否有递归？
- [x] B3. MomentumRank：period > len(df) 时是否跳过？返回空 Series 时 rank() 是否安全？
- [x] B4. VolumeRank：volume 列缺失时是否跳过？
- [x] B5. ReverseVolatilityRank：pct_change() 在只有 1 个数据点时返回全 NaN，std() 是否 NaN？
- [x] B6. EP/BP：PE<0/PB<0 已排除。PE=None 或 PE=NaN 是否正确跳过？
- [x] B7. SP/DP：PS_TTM=0 或 dv_ratio=None 是否正确跳过？
- [x] B8. ROE/ROA/GrossMargin/NetProfitMargin：fina_indicator 无数据时返回空，是否正确？
- [x] B9. Growth 因子：revenue_yoy/profit_yoy/roe_yoy 为 None 时是否跳过？
- [x] B10. Size 因子：total_mv=0 或 circ_mv=0 时 log(0) 是否被防护？
- [x] B11. AmihudIlliquidity：volume=0 的日期是否正确过滤？division by zero？
- [x] B12. IndustryMomentum：store=None 时是否返回空？行业映射全空时？
- [x] B13. FundamentalCrossFactor 是否从 registry pop 成功？（检查 import 顺序）
- [x] B14. AlphaCombiner 是否从 registry pop 成功？
- [x] B15. higher_is_better=False 的因子，compute_raw() 取负后，中性化和合成方向是否一致？

### C. 行业中性化 (ez/portfolio/neutralization.py)
- [x] C1. coverage=0（全部无行业）时是否正确跳过？
- [x] C2. 全部同一行业时，中性化后 sum=0，排名是否有意义？
- [x] C3. 行业均值含 NaN 时 transform('mean') 行为是否正确？
- [x] C4. dropna() 后返回的 Series index 是否与原始对齐？
- [x] C5. NeutralizedWrapper 多次 compute_raw() 调用，warnings 是否正确累积去重？

### D. AlphaCombiner (ez/portfolio/alpha_combiner.py)
- [x] D1. z-score 标准化：全部同值（std=0）→ 全 0，是否符合预期？
- [x] D2. DataFrame 外连接：不同因子覆盖不同股票，NaN 位置是否正确处理？
- [x] D3. 权重归一化：分母=0（所有因子对某股票都 NaN）→ NaN → dropna()，是否正确？
- [x] D4. legacy factor 检测：type(f).compute_raw is not CrossSectionalFactor.compute_raw 是否准确？（继承链干扰？）
- [x] D5. weights dict 为空 dict（不是 None）时行为：等权还是全零？
- [x] D6. 单因子合成：只有 1 个子因子时，结果是否等价于直接用该因子？
- [x] D7. name 属性拼接：子因子含特殊字符（如 +）时 name 是否安全？

### E. 截面评估器 (ez/portfolio/cross_evaluator.py)
- [x] E1. IC 计算：np.corrcoef 在 n<3 时行为？（已有 len(s)<5 guard）
- [x] E2. Rank IC：stats.spearmanr 返回结构在不同 scipy 版本是否一致？（.statistic vs .correlation）
- [x] E3. IC 衰减：lag>数据长度时是否返回 NaN？
- [x] E4. 分位数收益：pd.qcut 在有大量重复值时（相同排名）行为？duplicates='drop'？
- [x] E5. 因子相关性：两因子在同一日期无重叠股票时，相关系数是否 NaN？
- [x] E6. nanmean/nanstd：全部为 NaN 时是否返回 NaN（不是 0）？
- [x] E7. eval_dates 和 ic_series 长度是否始终一致？
- [x] E8. forward return：前向 N 天超出数据范围时是否正确返回 NaN？

### F. Walk-Forward (ez/portfolio/walk_forward.py)
- [x] F1. fold 划分：n_splits=2, train_ratio=0.9 时最后一个测试集是否太小？
- [x] F2. strategy_factory：是否真正创建新实例？（如果用户传了 lambda: same_instance？）
- [x] F3. OOS equity chaining：fold 间净值是否连续？（fold 2 起点 = fold 1 终点）
- [x] F4. compound return：产品累乘是否处理了负收益？（1 + (-1.5) = -0.5 → 负净值？）
- [x] F5. Monte Carlo：null hypothesis 是否正确（均值中心化 excess returns）？
- [x] F6. Bootstrap CI：样本量 < 30 时 CI 是否有意义？
- [x] F7. n_splits=2 边界：是否能正确运行？（最小有效值）

### G. FundamentalStore (ez/data/fundamental.py)
- [x] G1. PIT 查询：max(end_date) 在有重报时是否返回最新版本？
- [x] G2. preload additive：并发请求 A 加载 [S1,S2]，请求 B 加载 [S3,S4]，A 读 S1 是否安全？
- [x] G3. threading.Lock：preload 持锁期间 DuckDB 查询时间长，是否阻塞其他请求？
- [x] G4. daily_basic 5 天回退：连续假期 > 5 天（如春节）时是否返回 None？
- [x] G5. has_daily_basic 覆盖率 50%：新上市股票（半年数据 vs 3 年范围）是否总被判为"不足"？
- [x] G6. ann_date INDEX：DuckDB 对 CREATE INDEX IF NOT EXISTS 是否幂等？多次调用？
- [x] G7. save_daily_basic count：ON CONFLICT DO NOTHING + count before/after，并发写入时 count 是否准确？
- [x] G8. fina_indicator ON CONFLICT DO UPDATE：是否正确更新 ann_date？（重报场景）

### H. TushareProvider (ez/data/providers/tushare_provider.py)
- [x] H1. get_fina_indicator：字段名映射 q_revenue_yoy → revenue_yoy 是否正确？
- [x] H2. rate limit：0.35s throttle 在批量获取 50 只股票时是否导致 17.5s+ 延迟？
- [x] H3. token 为空时各方法是否一致抛出 ProviderError？
- [x] H4. 日期转换：_tushare_to_date 对非法日期字符串是否有防护？

### I. API 路由 (ez/api/routes/portfolio.py + fundamental.py)
- [x] I1. _get_factor_map() 在每次 API 调用时重新构建：是否有性能问题？（实例化所有因子）
- [x] I2. _create_strategy 所有路径是否都返回 (strategy, warnings) tuple？
- [x] I3. _create_alpha_combiner：skip_ensure 参数是否正确透传？
- [x] I4. _compute_alpha_weights：训练窗口数据不足时（start 很早）是否优雅降级？
- [x] I5. portfolio_search：max_combinations=200 × 50 stocks × 5 years = 运行时间？是否需要超时？
- [x] I6. evaluate-factors neutralize=True 但 Tushare 未配置时，industry_map 为空，是否 warning？
- [x] I7. /fundamental/fetch：symbols 列表为空时是否 400？
- [x] I8. /fundamental/quality：preload 后 industry_cache 是否正确填充？
- [x] I9. run_portfolio 响应的 warnings 字段：fund_warnings 为空列表时是否返回 null 还是 []？
- [x] I10. _NeutralizedWrapper 是否从 registry pop？（每次导入模块时执行）
- [x] I11. alpha_combiner 在 /evaluate-factors 的 _resolve_factors 中是否被正确拒绝？

### J. 前端 (web/src/components/)
- [x] J1. PortfolioPanel：factorCategories 为空时因子按钮是否正确 fallback 到 flat list？
- [x] J2. 中性化开关：neutralize=true 时前端是否正确传参给 API？
- [x] J3. AlphaCombiner UI：选了"多因子合成"但未选子因子时点击运行→ 是否有前端验证？
- [x] J4. 参数搜索：searchTopNs 输入 "abc" → parseInt → NaN → filter → 空数组 → 是否有提示？
- [x] J5. 多回测对比：selectedRuns 超过 10 个时是否有上限提示？
- [x] J6. CSV 导出：equity_curve 为空时导出是否报错？
- [x] J7. qualityReport useEffect 清空：symbols 变化时是否正确触发？（依赖数组正确？）
- [x] J8. FACTOR_LABELS：新增自定义因子时 fallback 到 raw key 是否正确显示？
- [x] J9. CATEGORY_LABELS：新增分类时 fallback 到 cat.label 是否正确？
- [x] J10. DateRangePicker：startDate > endDate 时是否有前端验证？
- [x] J11. 图表 ECharts：ic_series 含 null 时 ECharts line chart 是否正确跳过？
- [x] J12. 搜索结果表：sharpe 为 null 时显示是否为 '-' 而非 'null'？

### K. 数据一致性
- [x] K1. pyproject.toml / app.py / health endpoint / test 版本号是否一致？
- [x] K2. CLAUDE.md 测试数/版本号是否与实际一致？
- [x] K3. DocsPage 章节数是否与实际一致？
- [x] K4. roadmap 所有 [x] 是否真正实现？（已验证 27/27）
- [x] K5. roadmap 所有 [ ] 是否合理（不存在已实现但未勾选的项）？
- [x] K6. Known Limitations 是否完整？

### L. 安全与沙箱 (ez/agent/sandbox.py)
- [x] L1. dict-style dunder access 拦截是否覆盖 vars()["__import__"]？
- [x] L2. gc 模块是否在 _FORBIDDEN_MODULES 中？
- [x] L3. factor 保存：主进程是否不再 exec_module？
- [x] L4. strategy/portfolio_strategy 保存：仍在主进程 exec_module — 是否有文档说明？
- [x] L5. __dict__ 是否从 _SAFE_DUNDERS 中移除？

### M. 测试覆盖
- [x] M1. 组合引擎：空 universe 场景是否有测试？
- [x] M2. AlphaCombiner：legacy factor 在合成中的行为是否有测试？
- [x] M3. 参数搜索：MultiFactorRotation + 基本面因子场景是否有测试？
- [x] M4. 中性化：覆盖率恰好 50% 边界是否有测试？
- [x] M5. IC 评估：全部 NaN 输入时是否有测试？
- [x] M6. PIT 查询：重报（同一 end_date 不同 ann_date）是否有测试？
- [x] M7. 前端：无集成测试（React Testing Library），是否依赖 TS 类型检查 + 手动？

---

## 执行方式

1. 按 section (A-M) 逐项检查，用 agent 并行审查
2. 每发现一个 bug 立即修，标记 ✅
3. 确认无 bug 的标记 ✅ PASS
4. 全部完成后生成修复清单 + commit
