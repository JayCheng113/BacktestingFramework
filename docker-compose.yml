services:
  ez-trading:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data          # persist database
      - ./strategies:/app/strategies  # user strategies
    env_file:
      - path: .env
        required: false           # OK if .env doesn't exist
    restart: unless-stopped
